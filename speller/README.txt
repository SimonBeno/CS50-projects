Speller is a simple program in C that spell-checks a textfile based on a provided dictionary.
It uses pointers, memory allocation and nodes to load dictionary into a hash list. 
In this project, I experimented of how to create the hash list so that the program has the shortest checking time.
