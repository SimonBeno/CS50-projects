Filter is a program in C that can take any image and apply four different filters to it.
It can convert images to grayscale, sepia and revert or blur the image.
It relies on getting rgb values of every pixel of the image and then 
calculating values using different algorithms to apply the filter.
